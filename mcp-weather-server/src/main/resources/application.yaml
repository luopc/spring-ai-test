server:
  port: 8010
  servlet:
    session:
      timeout: 120s

spring:
  application:
    name: mcp-server
  ai:
    mcp:
      server:
        type: ASYNC
        sse-message-endpoint: /mcp/messages